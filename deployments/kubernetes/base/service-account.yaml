apiVersion: v1
kind: ServiceAccount
metadata:
  name: im-server
  labels:
    app.kubernetes.io/name: im-server
# 默认不授予 Kubernetes API 权限；证书由外部 CSI Provider 按工作负载身份注入。
automountServiceAccountToken: false

